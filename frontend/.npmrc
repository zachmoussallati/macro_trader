# Node 20.18.0 is the supported target (see .nvmrc + package.json engines).
# engine-strict=false lets the project install on neighbouring majors while
# still documenting the canonical version.
engine-strict=false
